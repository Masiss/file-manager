{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "progress",
    "description": "Capability for the progress window",
    "windows": [
        "progress"
    ],
    "permissions": [
        "core:default",
        "fs:default",
        "fs:allow-exists",
        "fs:allow-app-read",
        "core:window:allow-close"
    ]
}
