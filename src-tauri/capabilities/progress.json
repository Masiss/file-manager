{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "progress",
    "description": "Capability for the main window",
    "windows": [
        "progress"
    ],
    "permissions": [
        "core:default",
        "fs:default",
        "fs:allow-exists",
        "fs:allow-app-read"
    ]
}
